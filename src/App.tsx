import styled from "styled-components";

const ContainerDiv = styled.div`
    margin: 0;
    min-height: 100vh;
`;

function App(): JSX.Element {
    return (
        <ContainerDiv>
        </ContainerDiv>
    );
}

export default App;
